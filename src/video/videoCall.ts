// import React from 'react';
// import { View } from 'react-native';
// import { RTCView } from 'react-native-webrtc';

// const VideoCall = ({ localStream, remoteStream }: { localStream: MediaStream; remoteStream: MediaStream }) => {
//     const styles = {
//         container: { flex: 1, justifyContent: 'center', alignItems: 'center' },
//         video: { width: 300, height: 400, margin: 10 },
//     };

//     return (
//         <View style={styles.container}>
//             {localStream && <RTCView streamURL={localStream.toURL()} style={styles.video} />}
//             {remoteStream && <RTCView streamURL={remoteStream.toURL()} style={styles.video} />}
//         </View>
//     );
// };

// export default VideoCall;
